# AXON Mosquitto configuration — local development
# Production security (TLS, auth, ACLs) is future work (Phase 7+).

listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
log_dest stdout
log_type all

# Future: password_file, acl_file, TLS listeners
