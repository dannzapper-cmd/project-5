// AXON dashboard runtime config (Phase 1)
// Browser connects to host-accessible URLs, not Docker internal names.
window.AXON_CONFIG = {
  apiBase: "http://localhost:8000",
  wsBase: "ws://localhost:8000",
};
